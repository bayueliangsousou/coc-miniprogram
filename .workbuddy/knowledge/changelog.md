# 版本变更日志

## v1.4.0 (2026)
- 角色卡查看页UI优化
- 移除冗余 pages/dice 目录
- 骰子功能抽离为 dice-tool 共享组件
- P0-P4 代码质量优化完成
- P5 角色详情页 UI 细化进行中

## 2026-07-11
- **fix: 角色卡编辑未存档丢失（草稿双写）** — 根因：`character-edit` 页编辑态只存于 `this.data.character`，`saveCharacter` 仅在「跳转子页前」和「点保存」时触发；在主页改了姓名/属性/武器后若 App 被系统回收或划掉（未跳转、未保存），改动全部丢失。修复：`utils/character.js` 新增草稿 API（纯本地、不触发云端）：`saveDraft/loadDraft/clearDraft`（已存档角色，key `coc_draft_<id>`）+ `saveNewDraft/loadNewDraft/clearNewDraft`（尚未首次存档的新角色，固定 key `coc_draft_new`）+ `isDraftNewer`。`character-edit.js` 在 `onHide`/`onUnload` 时 `flushDraft()` 落地草稿；`onShow` 优先用比存档更新的草稿还原并 `toast` 提示「已恢复未保存的草稿」；`onSave` 成功后清草稿。涉及文件：`utils/character.js`、`pages/character-edit/character-edit.js`
- **fix: 草稿双写扩展到 skills/background/occupation 三个编辑子页** — 用户实测编辑技能→后台切出切回→技能回归基础数值。根因：①草稿双写仅挂 character-edit 主页，子页未接；②`skills.js` `onShow`→`initSkills` 无条件 `getCharacterById` 重建覆盖编辑态；③编辑动作只改 `skillCategories.current` 不回写 `character.skills`（展示态/数据态断层，落草稿也存不到真值）；④此前误判"子页已有自动保存"（子页 `saveCharacter` 仅 `onSave`/删自定义技能/信用评级 blur 时调用，无 `onHide` 自动保存）。修复：`saveDraft` 改**合并式**（基于已有草稿或存档 merge，避免三子页共用 `coc_draft_<id>` 互相覆盖）；`skills.js` `initSkills` 开头 `loadDraft` 优先合并草稿 skills、`onHide` 经 `collectSkills()` 收集技能值落草稿、`_doSave` 成功 `clearDraft`；`background.js`/`occupation.js` `onLoad` 优先合并草稿对应字段、`onHide` 落草稿、保存成功 `clearDraft`。涉及文件：`utils/character.js`、`pages/skills/skills.js`、`pages/background/background.js`、`pages/occupation/occupation.js`
- **feat: 新角色属性编辑区增加「总和」与「幸运单独投掷」** — 仅 `isNew`（新建角色）时显示，与现有「随机」按钮同标题行。①「总和：X」行：X = STR+CON+SIZ+DEX+APP+INT+POW+EDU 共8项之和（不含幸运），由 `recalcAttrSum()` 在 `onAttrInput`/`onRollAll`/`restoreCharacterData` 后实时重算 `attrSum`；②幸运格右侧「投」按钮（`onRollLuck`）：点击直接算 `3d6×5`（复用 `coc-data.rollDice`），写进 `character.attributes.LUK`，无动画无过程。编辑已有角色时不显示（沿用「随机」按钮 isNew 逻辑）。涉及文件：`pages/character-edit/character-edit.js`、`.wxml`、`.wxss`
- **fix: 角色详情页状态选择弹窗已选项无选中对号（根因修正）** — 现象：角色已挂「重伤」等状态（顶部徽标可见），再次打开「选择状态」弹窗，已选项前无对号。根因：**非样式问题**，而是选中判断依赖 `character.status` 与英文 key（`seriouslyInjured`）精确匹配；当 `character.status` 实际存的是中文 label（历史/PC同步/其他入口）时 `includes` 与 `selectedStatusMap` 查不到；且顶部徽标用 `statusLabels[item]`（英文→中文、无回退）只在英文 key 时才显示中文，不能作为判断依据。修复：①新增 `REVERSE_STATUS_LABEL`（中文→英文）+ `normalizeStatusList`，在 `onShow` 加载角色时把 `character.status` 统一规范化为英文 key 并 `saveCharacter` 存回；②`refreshSelectedStatusMap` 改为直接 `getCharacterById(id)` 读最新存档，且对每项同时建原值/英文 key 两个 map 入口，兼容中英文；③`onSelectStatus` 已选判断 `includes` 同时兼容中文 label。④选中态 UI 统一为绿色 `#52c41a`（实心圆+白「✓」+加粗绿色文字），不再按状态类型变化。涉及文件：`pages/character-detail/character-detail.js`、`.wxml`、`.wxss`
- **feat: 角色详情页技能检定弹窗增加「燃运（Burning Luck）」机制** — 仅当检定结果为「失败」（`calcSkillCheckResult` 返回 `class==='failure'`，**大失败 `fumble` 不触发**）时**在屏幕底部吸底显示**燃运区。消耗 = 投掷值 − 技能值（`rollValue − skillValue`，常规难度 ×1，app 鉴定投掷无困难/极难入口）。`onSkillCheckResultEvent` 计算 `showBurnLuck/ burnLuckCost/ luckEnough`；`luckEnough`（角色 `attributes.LUK >= cost`）→ 按钮可点、上方小字「减少幸运值上限 X」，`!luckEnough` → 按钮置灰不可点、小字「幸运值不足」；点击 `onBurnLuck` 真实扣减 `LUK` 并 `saveCharacter` 存盘，结果改为「燃运成功」、按钮消失，不弹新窗。关闭弹窗（`onCloseSkillCheckModal`/`onDiceToolClose`）复位燃运状态。按钮吸底固定于屏幕最下方、提示文字上方，样式**完全照搬首页「新建调查员」按钮**：`height: 88rpx`、`background: #c9a84c`、白字、`font-size: 30rpx`、`font-weight: 700`、`border-radius: 16rpx`、禁用态用同色系半透明背景。**燃运后即时刷新（修复认知错误）**：属性区幸运值读的是 `attrWithThresholds[item].value`（进页面时算一次的快照）而非 `character.attributes.LUK` 原值，原 `onBurnLuck` 只改 `character` 不动快照，关弹窗也未重算 → 燃运后数字不变。修复：新增 `recalcAttrThresholds(character)` 复用阈值算法；`onBurnLuck` 成功后与关闭弹窗（`onCloseSkillCheckModal`/`onDiceToolClose`）时均从 `getCharacterById(id)` 重读最新角色并 `setData({ character, attrWithThresholds })`，保证燃运后属性区幸运值立刻变新值。涉及文件：`pages/character-detail/character-detail.js`、`.wxml`（cover-view 结构）、`.wxss`
- **feat: 武器库「投掷」分组扩充** — 在 `utils/weapons-data.js` 的 `WEAPONS_BY_SKILL["投掷"]` 数组末尾追加 5 个投掷类武器（均按用户截图整理，时代 `1920s,现代`）：莫洛托夫鸡尾酒（damage `2D6+燃烧`）、炸药棒*（`4D10/3码`）、管状土制炸弹（`1D10/3码`）、塑胶炸药(C-4)（现代）（`6D10/3码`）、手榴弹*（`4D10/3码`）。用户要求全部归入「投掷」技能（原图技能列含「爆破」但用户确认归投掷），且命名按用户指定（如 C-4 改为「塑胶炸药(C-4)（现代）」）。仅存 `name/damage/era` 三字段，符合武器库既有规范。涉及文件：`utils/weapons-data.js`
- **chore: 武器库「爆破」分组移除水雷、鱼雷** — 用户指令从 `WEAPONS_BY_SKILL["爆破"]` 删除「水雷」（damage 4D10）与「鱼雷」（damage 4D10）两项。剩余爆破分组 8 项：定时炸弹、遥控炸弹（现代）、诡雷（现代）、地雷、深水炸弹、反坦克地雷（现代）、阔剑地雷（现代）、C4 炸药（现代）。涉及文件：`utils/weapons-data.js`
- **chore: 武器库「投掷（矛）」分组并入「投掷」并删除原分类** — 将 `WEAPONS_BY_SKILL["投掷（矛）"]` 的 4 项（短矛、长矛、三叉戟、鱼叉）合并进 `WEAPONS_BY_SKILL["投掷"]`；其中「长矛」（1D8/罕见）在「投掷」中已存在，去重不重复添加。最终「投掷」分组新增 短矛（1D6/罕见）、三叉戟（1D8+1/罕见）、鱼叉（1D8/1920s,现代）三项；「投掷（矛）」分类整体移除。涉及文件：`utils/weapons-data.js`
- **feat: 武器库「格斗（斗殴）」分组扩充 10 项** — 基于用户两张截图整理，在 `WEAPONS_BY_SKILL["格斗（斗殴）"]` 原「徒手」「指虎」基础上追加 10 项：燃烧的火把（1D6+燃烧/1920s,现代）、包革金属棒（大头棍、护身棒）（1D8+DB/1920s,现代）、大型棍棒（棒球棒、板球棒、拨火棍）（1D8+DB/1920s,现代）、小型棍棒（警棍）（1D6+DB/1920s,现代）、大型刀具（弯刀等）（1D8+DB/1920s,现代）、中型刀具（切肉刀等）（1D4+2+DB/1920s,现代）、小型刀具（折叠刀等）（1D4+DB/1920s,现代）、220V通电导线（2D8+眩晕/现代）、催泪喷雾*（晕眩/1920s,现代）、电击器*（1D3+晕眩/现代）。截图「锥肉喷雾*」按通用武器名修正为「催泪喷雾*」。仅存 `name/damage/era` 三字段。涉及文件：`utils/weapons-data.js`
- **fix: 武器库「射击（步枪/霰弹枪）」分组按 COC 规则书校正** — 用户对照规则书截图，修正差异：①`.45 马提尼-亨利步枪` damage `1D10+4`→`1D8+1D6+3`；②`.444 马林步枪（现代）` damage `2D8`→`2D8+4`；③`7.62mm M1 加兰德步枪` 改名 `加兰德 M1、M2 步枪`、damage `2D6+1`→`2D6+4`、era `1920s`→`二战及以后`；④`.303 李-恩菲尔德步枪` damage `2D6+1`→`2D6+4`、era `1920s`→`1920s,现代`；⑤`.30-06 斯普林菲尔德 M1903` 改名 `.30-06(7.62mm) 栓动步枪`、damage `2D6+1`→`2D6+4`、era `1920s`→`1920s,现代`；⑥新增 4 项缺失武器：`.58 春田燧发步枪`（`1D10+4`/罕见）、`莫兰上校的气动步枪`（`2D6+1`/1920s）、`.30-06(7.62mm) 半自动步枪`（`2D6+4`/现代）、`双管猎象枪`（`3D6+4`/1920s,现代）。仅 `name/damage/era` 三字段。涉及文件：`utils/weapons-data.js`

## 2026-07-12
- **refactor: 职业「警探」拆分为「警察」与「警探（原作向）」两个官方职业** — 原 `id:'police'` 名称「警探」数值四不像，按用户对照规则书截图拆分为官方两个职业：①`id:'police'` 改名「警察」：`creditRating [9,30]`、`pointFormula 'EDU × 2 + STR × 2 或 DEX × 2'`、技能 `格斗（斗殴）、射击、急救、一项社交技能（取悦、话术、恐吓、说服）、法律、心理学、侦察、驾驶（汽车）、骑乘、信用评级`（官方「格斗（拳击）」→小程序无拳击改用「格斗（斗殴）」；「汽车驾驶」→「驾驶（汽车）」、「骑术」→「骑乘」）；②新增 `id:'detective'`「警探（原作向）」：`creditRating [20,50]`、`pointFormula 'EDU × 2 + STR × 2 或 DEX × 2'`、技能 `艺术与手艺（表演）、乔装、射击、法律、聆听、一项社交技能（取悦、话术、恐吓、说服）、心理学、侦察、点一门技能、信用评级`（官方「侦查」→小程序统一「侦察」；「自选一技能」→「点一门技能」标准占位符）。「射击」用裸名覆盖全部射击子类（沿用现有约定）。涉及文件：`utils/coc-data.js`
- **feat: 职业技能互斥机制（mutualExclusion）** — 解决官方「二选一」职业技（艺术与手艺（表演）或乔装、汽车驾驶或骑术）在小程序无法表达的问题。职业数据新增 `mutualExclusion: [['A','B'], ...]` 字段；`parseOccupationConfig` 透传该字段；`updateSkillDisplayState` 在算完每个技能 `displayAsOcc`（★）后遍历互斥对：若一方 `current > 50` 则摘除另一方 ★（并使其不计入职业技能点数池）。效果：互斥技能初始都★（均锁定），谁先超 50 另一方自动摘星；低于 50 时恢复双★。点数输入后 `onSkillInput` 第 596 行重算 `updateSkillDisplayState` 实时生效。本期接入两对：`['艺术与手艺（表演）','乔装']`（警探）、`['驾驶（汽车）','骑乘']`（警察）。涉及文件：`pages/skills/skills.js`
- **fix: 律师职业按官方规则书校正** — 用户对照截图指出差异：①技能库 `会计` 改名 `会计学`；②律师职业技能改为 `会计学、图书馆使用、法律、二项社交技能（取悦、话术、恐吓或说服）、心理学、自选二技能、信用评级`（移除原 `母语`，「点三门技能」改为官方「自选二技能」）；③`parseOccupationConfig` 新增对「自选X技能」占位符的解析。信用评级 `[30,80]`、公式 `EDU × 4` 保持不变。涉及文件：`utils/coc-data.js`、`pages/skills/skills.js`
- **feat: 新增「罪犯」职业（官方规则书）** — 用户指出职业表缺罪犯，按官方截图新增 `id:'criminal'`「罪犯」：`creditRating [5,65]`、`pointFormula 'EDU × 2 + DEX × 2 或 STR × 2'`；`character.js` 新增该公式分支（取 `Math.max(STR,DEX)`，与现有 `EDU × 2 + STR × 2 或 DEX × 2` 分支数值等价）。技能：`一项社交技能（取悦、话术、恐吓、说服）、心理学、侦察、潜行` + **「下列选四」机制**：`下列选四（估价、乔装、格斗、射击、锁匠、机械修理、妙手）`。官方名映射：易容→乔装、机械维修→机械修理、侦查→侦察、「格斗/射击」用裸名覆盖子类。技能库新增 `妙手`（`baseValue 10`、分类`技术`）填充列表。涉及文件：`utils/coc-data.js`（职业+妙手技能）、`utils/character.js`（公式）、`pages/skills/skills.js`（`parseOccupationConfig` 解析 `下列选X（...）`→`listLimits`；`updateSkillDisplayState` 计算组内 `current>50` 降序取前 N 为 `listOccupied`，接入 `displayAsOcc` 判断，并为组内技能加 `skillLimitText`）、`pages/skills/skills.wxml` + `skills.wxss`（渲染 `skillLimitText` 标签「下列选N」）。
- **feat: 补全「或」公式分支（DEX或POW / APP或POW / APP或DEX或STR）** — `utils/character.js` 的 `calcSkillPoints` 新增三个分支：①`EDU × 2 + DEX × 2 或 POW × 2`（`Math.max(DEX,POW)`，服务艺术家等）；②`EDU × 2 + APP × 2 或 POW × 2`（`Math.max(APP,POW)`，服务狂热者，**并修复 `coc-data.js` 某职业原公式未被识别、长期走默认 EDU×4 的既有 bug**）；③`EDU × 2 + APP × 2 或 DEX × 2 或 STR × 2`（`Math.max(APP,DEX,STR)`，三属性取大，服务流浪者）。语义与现有分支一致：「或」取较大属性 ×2 再加 EDU×2。涉及文件：`utils/character.js`
- **feat: 新增缺失职业（问题1：官方有、小程序无）6 个** — 按用户对照官方截图补齐：①`artist` 艺术家（CR[9,50]、`EDU×2+DEX×2或POW×2`、艺术与手艺（任一）/历史/博物学/一项社交技能/其他语言/心理学/侦察/自选二技能，历史·博物学二选一 `mutualExclusion`）；②`cleric` 神职人员（CR[9,60]、`EDU×4`、会计学/历史/图书馆使用/聆听/其他语言/一项社交技能/心理学/自选一技能）；③`drifter` 流浪者（CR[0,5]、`EDU×2+APP×2或DEX×2或STR×2`、攀爬/跳跃/聆听/导航/一项社交技能/潜行/自选二技能）；④`farmer` 农民（CR[9,30]、`EDU×2+DEX×2或STR×2`、艺术与手艺（任一）/驾驶（汽车）/一项社交技能/机械修理/博物学/操作重型机械/追踪/自选一技能）；⑤`tribesman` 部落成员（CR[0,15]、`EDU×2+DEX×2或STR×2`、攀爬/格斗（斗殴）/投掷/博物学/聆听/神秘学/侦察/游泳/生存，格斗·投掷二选一 `mutualExclusion`）；⑥`fanatic` 狂热者（CR[0,30]、`EDU×2+APP×2或POW×2`、历史/二项社交技能/心理学/潜行/自选三技能）。官方名映射：侦查→侦察、汽车驾驶→驾驶（汽车）、机械维修→机械修理、社交技能→一项社交技能占位符、「生存（任一）」→裸名「生存」、「艺术与手艺（农事）」→「艺术与手艺（任一）」占位符（小程序无农事子类，玩家任选含农事）。涉及文件：`utils/coc-data.js`
- **fix: 技能库补齐与改名** — ①新增 `博物学` 技能（`baseValue 10`、分类`知识`，服务艺术家/农民/部落成员）；②**补全前次律师校正漏改的技能库定义**：`会计`→`会计学`（原律师校正只改了职业 skills 引用，技能库定义仍是旧名 `会计`，导致引用悬空），同步将 `会计师` 职业 skills 引用 `会计`→`会计学` 保持一致。涉及文件：`utils/coc-data.js`
- **refactor: 跨类别选多（下列选N）改减法模型 + 进度标签** — 按 ADR-002 最终边界，罪犯「下列选四」从"加法模型（候选初始不★、逐技能挂丑标签「下列选N」）"改为"减法模型 + 进度标签"。①`pages/skills/skills.js` 新增 `skillBaseMatch`（按基础名前缀匹配，兼容「射击」裸名覆盖全部射击子类）；`updateSkillDisplayState` 的 step 1.5 重写：候选人（跨分类）初始即全★（`inList` 标记），`current>50` 的按值降序只保留前 N 个★、其余摘星，并算整组占用数 `X = min(over50数, N)`；每个候选技能名后显示进度标签 `(可选职业技能X/N)`（替代丑标签「下列选N」）。②`utils/coc-data.js` 罪犯候选名单对齐实际技能名：`格斗`→`格斗（斗殴）`（「射击」保持裸名走前缀匹配）。③去除了 wxml 旧的 `（下列选N）` 标签文案，沿用 `.skill-limit-tag` 样式（金色 `#835F2D`）。当前小程序仅「罪犯」一个职业使用跨类别选多；社交四选一/艺术任一/科学选二（分类型）按用户确认保留旧加法逻辑不动。涉及文件：`pages/skills/skills.js`、`utils/coc-data.js`、`pages/skills/skills.wxml`（标签文案变动）
- **refactor: 职业数据 skillSpec 声明式收敛 + 机制字段统一（ADR-002 重构方案 B 落地）** — 将 43 个职业的中文串 `skills:[...]`（靠正则解析）整体迁移为纯声明式 `skillSpec:{locked, chooseFrom, mutualExclusion, categoryLimits, chooseAny}`，解析层退化成直读。①`utils/coc-data.js`：删 `parseChineseNum` 依赖、`listLimits`/`optionalCount` 旧字段；新增 `getOccupationSkillNames(spec)` 共享导出（含 locked+chooseFrom+mutualExclusion 成员 + 分类占位串 `艺术与手艺（任一）`/`科学（专业，两种）`/`一项社交技能（...）`，等价旧 `occ.skills` 前缀匹配行为）；②`pages/skills/skills.js`：`parseOccupationConfig` 直读 `skillSpec` 返回 `{lockedSkills, chooseFrom, mutualExclusion, categoryLimits, chooseAny}`，删 `parseChineseNum`；`updateSkillDisplayState` 用 `chooseFrom`（减法，原 `listLimits`）、`mutualExclusion`（独立减法，补 `mutualMembers` 初始双★逻辑）、`categoryLimits`（加法）、`chooseAny`（加法，原 `optionalCount`）；`isOccSkill` 改用 `getOccupationSkillNames`；③`utils/character.js`：`calcSkillPoints` 的 `occSkillNames` 改用 `getOccupationSkillNames(spec)`（社交仍走 `extraOccSkills`）；④`pages/occupation/occupation.js` 新增 `buildSkillChips(spec)` 把 `skillSpec` 转友好标签（`二选一：A / B` / `下列选N：…` / `社交选N` / `自选N项技能`），`occupation.wxml` 渲染 `item.skillChips`。**关键决策：`mutualExclusion` 保留为独立机制**（不与 `chooseFrom{count:1}` 合并），因互斥要求"一方>50 即强制摘另一方★"而 chooseFrom 仅摘"超名额候选"。校验脚本扫描 43 职业发现并修复护士 `locked` 悬空名「听觉」→「聆听」。所有文件 `node --check` 通过。**未 commit，待微信开发者工具重编译验证。**
- **fix: 职业技能加点上限判定 bug（社交选N 覆盖自由选N）** — `pages/skills/skills.js` 原 `onSkillInput` 用单一 `remainingSlots = chooseAny - 所有★技能数` 判定职业技能上限85，导致社交分类占用被错误扣减「自由选 N」（chooseAny）剩余名额：如律师「社交选2 + 自由选2」，选满社交2后自由选技能再加到>50 被判为兴趣技能（上限50），即"社交覆盖自由选2"。修复：改为"假设本技能加到最终值后重跑 `updateSkillDisplayState`，取本技能按所属机制是否成为★"作为上限判定，四类机制（锁定/名单型减法/互斥减法/分类型加法/全选多加法）各自独立、占用互不串味。并修复分类提示文案被 `chooseAny` 提示整体覆盖的问题（社交显示「{分类}技能需选N」、自由显示「所有技能里另选N个」）。临时脚本加载真实代码验证：律师社交选2+自由选2 四个技能独立获得★且上限均可达85。涉及文件：`pages/skills/skills.js`
- **fix: 按官方玩家手册截图整体校正 28 个职业** — 用户贴官方「玩家手册.jpg」，逐职业与 `coc-data.js` 当前 `skillSpec` 比对生成差异表（`/Users/liuqilong/WorkBuddy/2026-07-10-09-25-01/occupation_diff_table.md`），一次性修正：①**新增职业** `entertainer` 艺人（`EDU×2+APP×2`、艺术与手艺（表演）/乔装/聆听/心理学 + 社交选2 + 自选2）；②**新增技能** `艺术与手艺（设计图纸）`（工程师）、`艺术与手艺（农事）`（农民）、`科学（工程学）`（工程师）；③**职业重命名**：古物研究者→古文物学家/古董收藏家、军人→军官、图书管理员→图书馆管理员、花花公子→业余艺术爱好者、私人侦探→私家侦探、超自然研究员→心理学家、大学教授→教授、警探（原作向）→警探；④**大修**：工程师（新增设计图纸+工程学/物理学二选一+自选1）、农民（农事+汽车/骑乘二选一）、士兵（CR 9-30；闪避/格斗/射击/潜行/生存 + 攀爬/游泳二选一 + 急救/机械修理/其他语言选二，去掉投掷）、军官（会计学/射击/导航/心理学/生存+社交选2+自选1）、音乐家（公式改 `EDU×2+DEX×2或POW×2`、去掉唱歌/侦察、加聆听）、业余艺术爱好者（艺术与手艺（任一）/射击/其他语言/骑乘+社交选1+自选3）、私家侦探（去格斗/射击+社交选1+自选1）、飞行员（去驾驶（汽车）+加科学（天文学））；⑤**中等修正**：黑客（自选2）、记者（加其他语言+社交选1+自选2）、图书馆管理员（加会计学+自选4）、运动员（加自选1）、医生（去图书馆使用+加其他语言）、作家（母语+博物学/神秘学二选一+自选1）、古文物学家（艺术与手艺（任一）锁定+社交选1）、心理学家（自选1+加其他语言）；⑥**仅移除多余分类**：艺术家去掉 `categoryLimits.艺术`。**同步修两个真实 bug**：a) `character.js` `calcSkillPoints` 公式分支只认 `APP × 2 + EDU × 2`，不认 `EDU × 2 + APP × 2`（偶像、艺人职业点会掉回默认 EDU×4），合并两种等价写法；b) 同函数解构属性漏 `POW`，导致 `EDU×2+DEX×2或POW×2`/`EDU×2+APP×2或POW×2`（艺术家、狂热者、音乐家）计算职业点时 `ReferenceError` 崩溃，补 `POW = 0`。校验脚本确认 44 职业 / 81 技能 0 悬空名（分类占位串社交项已排除），`node --check` 全过。涉及文件：`utils/coc-data.js`、`utils/character.js`。**未 commit，待微信开发者工具重编译验证。**
- **fix: 分类型选多（categoryLimits）分类标题标注改为实时「可选X」** — 原 `pages/skills/skills.js` `updateSkillDisplayState` 对 `categoryLimits`（社交/艺术/科学选N）的分类标题 `limitText` 写死为静态 `（{分类}技能需选{catLimit}）`，从不随实际已选数递减（用户反馈：社交选2 后标注仍显示「需选2」、不到 0）。修复：改为实时算该分类已选数（`categoryOccupied` 按名额封顶后的个数），显示剩余可选数 `（{分类}技能可选{max(0, catLimit - 已选)}）`，选满归 0。验证：加载真实代码模拟社交选2，标注依次 `可选2`→`可选1`→`可选0`（超额仍封顶 0）。注：跨类别 `chooseFrom` 的技能级标签仍为 `可选职业技能X/N`（X=已选数，封顶 N），与分类标题「可选X=剩余」表达口径不同，后续如需统一再调整。涉及文件：`pages/skills/skills.js`。**未 commit。**
- **fix: 合并近义技能 电器修理/电气维修 → 电器维修** — 技能库原有两个重复近义技能 `电器修理`（技术，baseValue 10）与 `电气维修`（技术，baseValue 10），分别被不同职业引用（工程师/飞行员/黑客等）。统一合并为单个 `电器维修`（baseValue 10，技术）：删除 `电气维修` 定义项、将 `电器修理` 定义改名为 `电器维修`，并把全部职业 `skillSpec.locked` 中的 `电器修理`/`电气维修` 引用替换为 `电器维修`。校验：SKILLS 总数 81→80、无重复名、`电器维修` 存在、`node --check` 通过。涉及文件：`utils/coc-data.js`。**未 commit。**
- **fix: 删除孤儿技能 投掷（矛）** — 用户多次要求把「投掷（矛）」武器并入「投掷」并删掉该分类。武器合并此前已完成（`utils/weapons-data.js` 的 `投掷（矛）` 分类已不存在，矛类武器均在 `投掷` 下），但技能库 `utils/coc-data.js` 仍残留一个同名孤儿技能 `投掷（矛）`（baseValue 20，战斗，与 `投掷` 完全重复，无任何职业引用）。本次删除该孤立技能定义，保留 `投掷`。校验：`投掷（矛）` 全项目无残留、`投掷` 存在、SKILLS 无重复、`node --check` 通过。涉及文件：`utils/coc-data.js`。**未 commit。**
- **fix: 修正 13 职业艺术/科学/战斗★声明 + 删假技能 + 修 chooseFrom 摘星** — 用户复核各职业标星集合后指出：①`艺术与手艺（任一）` 不是真实技能名，是「从艺术分类选 N」的占位符，应表达为 `categoryLimits:{艺术:N}`；传教士/艺术家/业余艺术爱好者/古文物学家 4 职业曾把它当 locked 技能用，改为 `categoryLimits:{艺术:1}`（保留各自 social/互斥/chooseAny）。②工程师 `科学（工程学）`/`科学（物理学）` 原在 `chooseFrom` 二选一，应为两者都恒★ → 移入 `locked`。③删除技能库 `艺术与手艺（任一）` 假技能定义（无任何职业再引用）。④`pages/skills/skills.js` `updateSkillDisplayState` 的 `chooseFrom` 摘星逻辑修正：原仅当候选 `current>50` 且超名额才摘星，导致士兵「急救/机械维修/其他语言 3选2」前两个>50 后第三个（<50）仍保留★；改为按组算 occupied 数，名额已满（occupied.length>=count）时组内未入选成员一律摘星（即便 current<=50），等价「选 N 选多」；罪犯 7选4「进入全★、超名额摘星」行为不变。⑤`getOccupationSkillNames` 的 catMap `艺术` 占位由 `艺术与手艺（任一）` 改为 `艺术与手艺`（基础名匹配等价，避免引用已删技能）。验证：加载真实 `skills.js` 写脚本断言 14 项全过（工程师科学(工程学,物理学)+艺术(设计图纸)、士兵3选2、艺术家/作家 历史·博物 与 博物·神秘 互斥双★+摘另一方、传教士/古文物/业余艺术选1、艺人(表演)/农民(农事)/记者(摄影)/音乐家(乐器)/警察(斗殴)/运动员(斗殴) 锁定★、假技能已删）；`getOccupationSkillNames` 对传教士返回不含 `艺术与手艺（任一）` 且 `艺术与手艺（绘画）` 正确识别为职业点。涉及文件：`utils/coc-data.js`、`pages/skills/skills.js`。**未 commit，待微信开发者工具重编译验证。**

