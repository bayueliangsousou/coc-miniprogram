/* pages/skills/skills.wxss */

/* 页面外层容器 */
.skills-page {
  display: flex;
  flex-direction: column;
  height: 100vh;
  overflow: hidden;
  box-sizing: border-box;
  background: #f5f5f0;
}

/* 滚动区域 */
.skills-scroll {
  flex: 1;
  height: 0;
  min-height: 0;
  padding: 0 24rpx;
  padding-bottom: 140rpx;
}

/* 社会信用评级 */
.credit-rating-section {
  background: #f8f7f3;
  border-radius: var(--radius-md);
  padding: 16rpx 20rpx;
  margin-bottom: 16rpx;
  border: 2rpx solid var(--color-border);
  box-sizing: border-box;
}

.credit-rating-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.credit-rating-title {
  font-size: 28rpx;
  font-weight: 700;
  color: var(--color-text);
}

.credit-rating-right {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.credit-rating-input {
  width: 100rpx;
  height: 52rpx;
  border: 2rpx solid var(--color-border);
  border-radius: var(--radius-sm);
  text-align: center;
  font-size: 28rpx;
  font-weight: 700;
  color: var(--color-text);
  background: #fff;
  font-family: 'Special Elite';
}

.credit-rating-input-error {
  border-color: #e74c3c !important;
  color: #e74c3c !important;
}

.credit-rating-error-text {
  color: #e74c3c !important;
}

.credit-rating-range {
  font-size: 24rpx;
  color: var(--color-primary-dark);
  background: rgba(201, 168, 76, 0.12);
  padding: 4rpx 12rpx;
  border-radius: 20rpx;
}

/* 技能点数面板 - 左右并排，固定在顶部 */
.points-panel {
  background: linear-gradient(135deg, #252533 0%, #2d2d4a 100%);
  border-radius: 0 0 var(--radius-md) var(--radius-md);
  padding: 16rpx 44rpx;
  margin: 0 0 16rpx;
  box-shadow: 0 4rpx 12rpx rgba(37, 37, 51, 0.2);
  display: flex;
  gap: 20rpx;
  flex-shrink: 0;
}

.points-section {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 4rpx;
}

.points-numbers {
  display: flex;
  align-items: baseline;
  gap: 6rpx;
  margin-bottom: 2rpx;
}

.points-title {
  font-size: 24rpx;
  font-weight: bold;
  color: rgba(255,255,255,0.9);
}

.points-formula {
  font-size: 20rpx;
  color: rgba(255,255,255,0.5);
  margin-bottom: 2rpx;
}

.points-remain {
  font-size: 42rpx;
  font-weight: bold;
  color: #c9a84c;
  line-height: 1;
  font-family: 'Special Elite';
}

.points-remain.points-negative {
  color: #e74c3c;
}

.points-slash {
  font-size: 28rpx;
  color: rgba(255,255,255,0.4);
  line-height: 1;
  font-family: 'Special Elite';
}

.points-total {
  font-size: 28rpx;
  color: rgba(255,255,255,0.5);
  line-height: 1;
  font-family: 'Special Elite';
}

.points-bar-wrap {
  height: 6rpx;
  background: rgba(255,255,255,0.1);
  border-radius: 3rpx;
  overflow: hidden;
}

.points-bar-fill {
  height: 100%;
  transition: width 0.3s ease;
}

.points-bar-fill.occ-fill {
  background: linear-gradient(90deg, #c9a84c, #8b5e1a);
}

.points-bar-fill.int-fill {
  background: linear-gradient(90deg, #3498db, #2980b9);
}

/* 职业和兴趣区域的特殊样式 */
.points-section.occ-section {
  padding-right: 20rpx;
}

.points-section.int-section {
  padding-left: 20rpx;
  border-left: 1rpx solid rgba(255,255,255,0.1);
}

.search-bar {
  display: flex;
  align-items: center;
  background: #fff;
  border-radius: var(--radius-md);
  padding: 0 20rpx;
  margin-bottom: 16rpx;
  border: 2rpx solid var(--color-border);
  height: 72rpx;
}

.search-icon { font-size: 28rpx; margin-right: 12rpx; }

.search-input {
  flex: 1;
  height: 72rpx;
  font-size: 28rpx;
  color: var(--color-text);
}

.search-nav {
  min-width: 80rpx;
  height: 48rpx;
  background: var(--color-primary);
  border-radius: 24rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0 16rpx;
  margin-left: 12rpx;
}

.search-nav-empty {
  background: #e0e0e0;
}

.search-count {
  font-size: 24rpx;
  color: #fff;
  font-weight: 600;
  white-space: nowrap;
}

.search-nav-empty .search-count {
  color: #888888;
}

/* 当前定位技能高亮 */
.skill-row.skill-highlight {
  background: rgba(201, 168, 76, 0.18);
  border-left: 4rpx solid var(--color-primary);
}

.occ-hint {
  font-size: 24rpx;
  color: var(--color-primary-dark);
  padding: 8rpx 12rpx;
  margin-bottom: 16rpx;
  display: flex;
  align-items: center;
  gap: 6rpx;
}

/* 分类标题 */
.cat-header {
  display: flex;
  align-items: center;
  padding: 16rpx 12rpx;
  background: rgba(201,168,76,0.08);
  border-radius: var(--radius-sm);
  margin-bottom: 4rpx;
  margin-top: 12rpx;
}

.cat-name {
  flex: 1;
  font-size: 24rpx;
  font-weight: 700;
  color: var(--color-text);
}

.cat-count {
  font-size: 20rpx;
  color: #888888;
  margin-right: 8rpx;
  font-family: 'Special Elite';
}

.cat-arrow {
  font-size: 24rpx;
  color: #888888;
}

/* 技能行 */
.skill-row {
  display: flex;
  align-items: center;
  padding: 14rpx 12rpx;
  background: #fff;
  border-bottom: 1rpx solid var(--color-border);
  position: relative;
}

.skill-name-wrap {
  flex: 1;
  display: flex;
  align-items: center;
  gap: 6rpx;
}

.occ-dot {
  color: #835F2D;
  font-size: 20rpx;
}

.skill-name {
  font-size: 28rpx;
  color: var(--color-text);
}

.skill-base {
  font-size: 20rpx;
  color: #888888;
  margin-left: 4rpx;
  font-family: 'Special Elite';
}

.skill-row.is-occ .skill-name {
  color: #835F2D;
}

.skill-row.is-occ .skill-base {
  color: #835F2D;
}

.skill-inputs {
  display: flex;
  align-items: center;
  gap: 10rpx;
}

.skill-input {
  width: 80rpx;
  height: 56rpx;
  border: 2rpx solid var(--color-border);
  border-radius: var(--radius-sm);
  text-align: center;
  font-size: 30rpx;
  font-weight: 700;
  color: var(--color-text);
  padding: 0;
  background: #fafafa;
  font-family: 'Special Elite';
}

.skill-input.input-occ {
  border-color: var(--color-primary);
  color: var(--color-primary-dark);
  background: rgba(201,168,76,0.06);
}

.skill-input.input-int {
  border-color: #3498db;
  color: #2980b9;
  background: rgba(52, 152, 219, 0.06);
}

.skill-input.input-error {
  border-color: #e74c3c;
  background: rgba(231, 76, 60, 0.05);
}

.skill-input.current {
  /* 保留旧样式 */
}

.skill-thresholds {
  display: flex;
  align-items: center;
  gap: 4rpx;
}

.threshold {
  font-size: 24rpx;
  color: var(--color-text-sub);
  min-width: 36rpx;
  text-align: center;
  font-family: 'Special Elite';
}

.threshold-sep {
  font-size: 20rpx;
  color: #ccc;
}

/* 顶部表头提示 */
.skills-header-hint {
  display: flex;
  justify-content: flex-end;
  font-size: 24rpx;
  color: #888888;
  padding: 0 12rpx;
  margin-bottom: 8rpx;
}

.bottom-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 16rpx 32rpx;
  padding-bottom: calc(16rpx + env(safe-area-inset-bottom));
  background: rgba(245, 245, 240, 0.95);
  backdrop-filter: blur(10px);
  box-shadow: 0 -2rpx 16rpx rgba(0, 0, 0, 0.08);
  z-index: 1000;
}

.bottom-btns {
  display: flex;
  gap: 20rpx;
}

.btn-random {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
}

.dice-icon {
  width: 36rpx;
  height: 36rpx;
  filter: drop-shadow(0 0 0 var(--color-primary-dark));
}

.btn-save {
  flex: 2;
}

.save-warning {
  text-align: center;
  font-size: 24rpx;
  color: #e74c3c;
  margin-bottom: 12rpx;
}

.btn-disabled {
  opacity: 0.5;
}

/* 语言名称输入（其他语言行） */
.language-name-input {
  width: 120rpx;
  height: 40rpx;
  border: 2rpx solid var(--color-border);
  border-radius: var(--radius-sm);
  padding: 0 10rpx;
  font-size: 28rpx;
  color: var(--color-text);
  background: #fff;
  margin-left: 8rpx;
}

/* 自定义技能行 */
.skill-row.is-custom {
  position: relative;
  padding-right: 60rpx;
}

/* 删除按钮 */
.skill-delete {
  display: inline-flex;
  align-items: center;
  padding: 2rpx 10rpx;
  margin-left: 8rpx;
}

.delete-icon {
  font-size: 22rpx;
  color: #e74c3c;
  line-height: 1;
}

/* 新技能按钮 */
.btn-new-skill {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 新建技能弹窗 */
.modal-mask {
  position: fixed;
  top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(0,0,0,0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 9999;
}

.modal-box {
  width: 600rpx;
  background: #fff;
  border-radius: 24rpx;
  padding: 40rpx 32rpx 32rpx;
}

.modal-title {
  font-size: 32rpx;
  font-weight: 700;
  color: var(--color-text);
  margin-bottom: 32rpx;
  text-align: center;
}

.new-skill-row {
  display: flex;
  align-items: center;
  gap: 16rpx;
  margin-bottom: 16rpx;
}

.new-skill-name-input {
  flex: 1;
  height: 80rpx;
  border: 2rpx solid var(--color-border);
  border-radius: var(--radius-sm);
  padding: 0 20rpx;
  font-size: 28rpx;
  color: var(--color-text);
  background: #fff;
  box-sizing: border-box;
}

.new-skill-val-input {
  width: 140rpx;
  height: 80rpx;
  border: 2rpx solid var(--color-border);
  border-radius: var(--radius-sm);
  padding: 0 20rpx;
  font-size: 28rpx;
  font-weight: 700;
  color: var(--color-text);
  background: #fff;
  text-align: center;
  box-sizing: border-box;
}

.new-skill-hint {
  font-size: 22rpx;
  color: var(--color-text-sub);
  margin-bottom: 32rpx;
}

.modal-btns {
  display: flex;
  gap: 20rpx;
}

.modal-btns .btn {
  flex: 1;
}
